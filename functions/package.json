{
  "name": "xponet-functions",
  "description": "Cloud Functions for the Xponet workspace (AI agent).",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:workspaceAgent",
    "logs": "firebase functions:log --only workspaceAgent"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  }
}
